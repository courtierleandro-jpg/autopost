name: Valider posts.csv

on:
  push:
    paths:
      - "posts.csv"
      - "images/**"
  pull_request:
    paths:
      - "posts.csv"
      - "images/**"
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Valider posts.csv
        run: cd src && python validate.py
